{
  "name": "galexp3",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "description": "Browser-based 4X space game (Master of Orion / Galactic Civilizations style).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "spec": "quint",
    "spec:test": "for f in quint/starmap.qnt quint/starSystem.qnt; do npx quint test --match='.*' \"$f\"; done",
    "spec:check": "for f in quint/galaxy.qnt quint/starmap.qnt; do npx quint typecheck \"$f\"; done"
  },
  "dependencies": {
    "pixi.js": "^8.19.0"
  },
  "devDependencies": {
    "@informalsystems/quint": "^0.22.0",
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
